.git/
.github/
.hyperexecute/
.testmu/
screenshots/
hyperexecute-cli.log
reports/html/
reports/junit/
# Per-scenario HE results from an earlier job must never ride up to the VMs — every VM
# would echo them back into the merged artefact as if they were fresh (Stage 6b).
reports/kane_result_*.json
reports/native_*.xml
reports/he_artifacts/
